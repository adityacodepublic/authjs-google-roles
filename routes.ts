export const publicRoutes = ["/error/too-many-requests", ""];

export const allowedCities = ["Mumbai", "Nashik", ""];

export const authRoutes = ["/signin"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_lOGIN_REDIRECT = "/reports/new";

const roleRoute = {
  superadmin: ["/"],
};
